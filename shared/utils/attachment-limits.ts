/** Maximum bytes accepted for a single uploaded attachment. */
export const MAX_ATTACHMENT_FILE_BYTES = 10 * 1024 * 1024

/** Maximum length of an attachment's original filename. */
export const MAX_ATTACHMENT_FILENAME_LENGTH = 255
